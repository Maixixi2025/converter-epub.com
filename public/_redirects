# Cloudflare Pages _redirects
# (sitemap.xml is now generated by scripts/alias-sitemap.mjs as a real static file —
#  no redirect needed. The old /sitemap.xml → /sitemap-index.xml 301 was a workaround
#  for the missing @astrojs/sitemap /sitemap.xml file. See skill astro-sitemap-alias.)
